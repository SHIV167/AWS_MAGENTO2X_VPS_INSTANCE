# Exclude Magento generated and cache directories from Docker build context
src/var
src/generated
src/pub/static
src/pub/media

# Optional: ignore vendor dependencies if mounted at runtime
src/vendor

# Exclude Docker ignore file itself
.dockerignore
